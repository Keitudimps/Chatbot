//! Re-exports and convenience wrappers for the Q&A model.
//!
//! The full model implementation lives in `transformer.rs`.
//! This module re-exports the key types so callers can use a single import path.
